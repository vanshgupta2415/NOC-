# System Architecture - No Dues Portal

## Overview
```
┌─────────────────────────────────────────────────────────────────┐
│                         USER BROWSER                             │
│                     http://localhost:5173                        │
└─────────────────────────────────────────────────────────────────┘
                              │
                              │ HTTP Requests
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                    FRONTEND (React + Vite)                       │
│                                                                  │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │  App.tsx (Entry Point)                                     │ │
│  │  └─ AuthProvider (Authentication Context)                  │ │
│  │     └─ BrowserRouter (Routing)                             │ │
│  │        └─ ProtectedRoute (Role-based Access)               │ │
│  │           └─ Pages (Student/Authority/Admin)               │ │
│  └────────────────────────────────────────────────────────────┘ │
│                              │                                   │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │  API Service Layer (src/lib/api.ts)                        │ │
│  │  • Axios instance with interceptors                        │ │
│  │  • Automatic token management                              │ │
│  │  • Token refresh on 401                                    │ │
│  │  • Type-safe API methods                                   │ │
│  └────────────────────────────────────────────────────────────┘ │
│                              │                                   │
└──────────────────────────────┼───────────────────────────────────┘
                               │
                               │ HTTP/JSON
                               │ Authorization: Bearer <token>
                               ▼
┌─────────────────────────────────────────────────────────────────┐
│                   BACKEND (Node.js + Express)                    │
│                     http://localhost:5000                        │
│                                                                  │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │  Middleware Stack                                          │ │
│  │  • CORS (allows frontend origin)                           │ │
│  │  • Helmet (security headers)                               │ │
│  │  • Rate Limiting (100 req/15min)                           │ │
│  │  • Body Parser (JSON/URL-encoded)                          │ │
│  │  • Cookie Parser                                           │ │
│  │  • Morgan (logging)                                        │ │
│  └────────────────────────────────────────────────────────────┘ │
│                              │                                   │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │  Routes                                                     │ │
│  │  • /api/auth       → authRoutes                            │ │
│  │  • /api/student    → studentRoutes                         │ │
│  │  • /api/approvals  → approvalRoutes                        │ │
│  │  • /api/admin      → adminRoutes                           │ │
│  └────────────────────────────────────────────────────────────┘ │
│                              │                                   │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │  Middleware (Auth)                                         │ │
│  │  • verifyToken - JWT validation                            │ │
│  │  • roleAuth - Role-based authorization                     │ │
│  └────────────────────────────────────────────────────────────┘ │
│                              │                                   │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │  Controllers                                               │ │
│  │  • authController - Login, logout, refresh                 │ │
│  │  • studentController - Applications, status, certificates  │ │
│  │  • approvalController - Approve, pause, view               │ │
│  │  • adminController - Users, stats, audit logs              │ │
│  └────────────────────────────────────────────────────────────┘ │
│                              │                                   │
└──────────────────────────────┼───────────────────────────────────┘
                               │
                               │ Mongoose ODM
                               ▼
┌─────────────────────────────────────────────────────────────────┐
│                    DATABASE (MongoDB)                            │
│                   mongodb://localhost:27017                      │
│                                                                  │
│  Collections:                                                    │
│  • users          - User accounts (Student, Faculty, Admin)     │
│  • applications   - No dues applications                        │
│  • certificates   - Generated certificates                      │
│  • auditlogs      - System activity logs                        │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

## Authentication Flow

```
┌──────────┐                                                    ┌──────────┐
│  Login   │                                                    │ Backend  │
│   Page   │                                                    │  Server  │
└────┬─────┘                                                    └────┬─────┘
     │                                                                │
     │ 1. User enters email & password                               │
     ├──────────────────────────────────────────────────────────────►│
     │    POST /api/auth/login                                       │
     │    { email, password }                                        │
     │                                                                │
     │                                   2. Validate credentials     │
     │                                   3. Generate JWT tokens      │
     │                                                                │
     │◄──────────────────────────────────────────────────────────────┤
     │    { accessToken, refreshToken, user }                        │
     │                                                                │
     │ 4. Store tokens in localStorage                               │
     │ 5. Store user in context                                      │
     │ 6. Navigate to role-based dashboard                           │
     │                                                                │
     │                                                                │
     │ 7. Subsequent requests include token                          │
     ├──────────────────────────────────────────────────────────────►│
     │    GET /api/student/application/status                        │
     │    Authorization: Bearer <accessToken>                        │
     │                                                                │
     │                                   8. Verify token              │
     │                                   9. Check permissions         │
     │                                   10. Return data              │
     │◄──────────────────────────────────────────────────────────────┤
     │    { status: "success", data: {...} }                         │
     │                                                                │
```

## Token Refresh Flow

```
┌──────────┐                                                    ┌──────────┐
│ Frontend │                                                    │ Backend  │
│   API    │                                                    │  Server  │
└────┬─────┘                                                    └────┬─────┘
     │                                                                │
     │ 1. Request with expired token                                 │
     ├──────────────────────────────────────────────────────────────►│
     │    GET /api/student/application                               │
     │    Authorization: Bearer <expired_token>                      │
     │                                                                │
     │◄──────────────────────────────────────────────────────────────┤
     │    401 Unauthorized                                           │
     │                                                                │
     │ 2. Axios interceptor catches 401                              │
     │ 3. Attempt token refresh                                      │
     ├──────────────────────────────────────────────────────────────►│
     │    POST /api/auth/refresh                                     │
     │    { refreshToken }                                           │
     │                                                                │
     │                                   4. Validate refresh token   │
     │                                   5. Generate new access token│
     │◄──────────────────────────────────────────────────────────────┤
     │    { accessToken }                                            │
     │                                                                │
     │ 6. Update localStorage                                        │
     │ 7. Retry original request with new token                      │
     ├──────────────────────────────────────────────────────────────►│
     │    GET /api/student/application                               │
     │    Authorization: Bearer <new_token>                          │
     │                                                                │
     │◄──────────────────────────────────────────────────────────────┤
     │    200 OK { data }                                            │
     │                                                                │
```

## Component Hierarchy

```
App
├── AuthProvider (Context)
│   ├── BrowserRouter
│   │   ├── Routes
│   │   │   ├── / → Index (Public)
│   │   │   ├── /login → Login (Public)
│   │   │   │
│   │   │   ├── /student → ProtectedRoute (Student)
│   │   │   │   └── StudentDashboard
│   │   │   │       ├── DashboardLayout
│   │   │   │       │   ├── Sidebar (nav, logout)
│   │   │   │       │   └── Header (user info)
│   │   │   │       └── Dashboard Content
│   │   │   │           ├── Welcome Banner
│   │   │   │           ├── Quick Actions
│   │   │   │           └── Approval Progress
│   │   │   │
│   │   │   ├── /student/apply → ProtectedRoute (Student)
│   │   │   │   └── StudentApply
│   │   │   │
│   │   │   ├── /authority → ProtectedRoute (Authority)
│   │   │   │   └── AuthorityDashboard
│   │   │   │
│   │   │   └── /admin → ProtectedRoute (SuperAdmin)
│   │   │       └── AdminDashboard
│   │   │
│   │   └── * → NotFound
```

## Data Flow Example: Student Dashboard

```
1. User navigates to /student
   ↓
2. ProtectedRoute checks authentication
   ↓
3. StudentDashboard component mounts
   ↓
4. useQuery triggers API call
   ↓
5. studentAPI.getApplicationStatus()
   ↓
6. Axios interceptor adds auth token
   ↓
7. HTTP GET → /api/student/application/status
   ↓
8. Backend verifyToken middleware
   ↓
9. Backend roleAuth middleware (check Student role)
   ↓
10. studentController.getApplicationStatus()
    ↓
11. Query MongoDB for application
    ↓
12. Return JSON response
    ↓
13. Axios interceptor processes response
    ↓
14. React Query caches data
    ↓
15. Component re-renders with data
    ↓
16. Display approval stages, progress, etc.
```

## File Upload Flow

```
Student → Form with files
   ↓
FormData object created
   ↓
studentAPI.submitApplication(formData)
   ↓
Axios sets Content-Type: multipart/form-data
   ↓
POST /api/student/application
   ↓
Backend multer middleware
   ↓
Files saved to ./uploads/
   ↓
File paths stored in MongoDB
   ↓
Response with application ID
   ↓
Frontend shows success message
```

## Role-Based Access Control

```
User Login
   ↓
Backend returns user with role
   ↓
Frontend stores in AuthContext
   ↓
ProtectedRoute checks allowedRoles
   ↓
┌─────────────────────────────────────┐
│ Role: Student                       │
│ Access: /student/*                  │
│ Denied: /authority/*, /admin/*      │
└─────────────────────────────────────┘
┌─────────────────────────────────────┐
│ Role: Faculty/HOD/etc               │
│ Access: /authority/*                │
│ Denied: /student/*, /admin/*        │
└─────────────────────────────────────┘
┌─────────────────────────────────────┐
│ Role: SuperAdmin                    │
│ Access: /admin/*, all routes        │
└─────────────────────────────────────┘
```

## Environment Configuration

```
Frontend (.env)
├── VITE_API_BASE_URL=http://localhost:5000/api
└── (Loaded via import.meta.env)

Backend (.env)
├── PORT=5000
├── MONGODB_URI=mongodb://localhost:27017/nodues-portal
├── CORS_ORIGIN=http://localhost:5173
├── JWT_ACCESS_SECRET=...
├── JWT_REFRESH_SECRET=...
└── (Loaded via dotenv)
```

---

This architecture ensures:
- ✅ Secure authentication with JWT
- ✅ Automatic token refresh
- ✅ Role-based access control
- ✅ Type-safe API calls
- ✅ Centralized state management
- ✅ Clean separation of concerns
